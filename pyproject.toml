[project]
name = "CTR_websockets"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "websockets>=12.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["serve"]

[tool.setupstools.packages.find]
where= ['src']